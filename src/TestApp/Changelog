Errors running Unit Tests are now logged
    
    it no longer gives "free A's"
Fixed Simple Unit Tests failing if the program was closed before the code for running it previously was removed.
Fixed Simple Unit Tests failing if there was already a main method.